name: Deploy

on:
  workflow_run:                # стартуем, когда отработал другой workflow
    workflows: [CI]            # имя из строки name: в ci.yml
    types: [completed]         # в момент, когда CI закончил работу
    branches: [main]           # и только если это был main

jobs:
  deploy:
    runs-on: ubuntu-latest
    # CI закончился — но чем? Едем на сервер только если зелёным
    if: github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Prepare image name
        run: |
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE=ghcr.io/$OWNER_LOWER/marketplace-service" >> $GITHUB_ENV

      - name: Deploy to server            # зайти на сервер по SSH и перезапустить контейнер
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}      # host/user/ключ — всё из секретов, в коде их нет
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # команды ниже выполняются НА сервере
            # логинимся в реестр, чтобы стянуть приватный образ
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # тянем образ, который CI уже собрал и проверил тестами
            docker pull ${{ env.IMAGE }}:latest

            # сносим старый контейнер, если был
            docker rm -f marketplace-satisfaction 2>/dev/null || true

            # поднимаем новый в фоне
            docker run -d \
              --name marketplace-satisfaction \
              --restart unless-stopped \
              -p 127.0.0.1:8000:8000 \
              ${{ env.IMAGE }}:latest

            # убеждаемся, что контейнер действительно жив, и только тогда чистим диск
            sleep 5
            if [ -n "$(docker ps -q -f name=marketplace-satisfaction -f status=running)" ]; then
              docker image prune -f
            else
              echo "Контейнер не поднялся"
              docker logs --tail 50 marketplace-satisfaction
              exit 1
            fi

            docker logout ghcr.io